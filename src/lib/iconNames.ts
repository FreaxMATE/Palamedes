/** Semantic names the chrome uses. Each maps to a Lucide icon in `Icon.svelte`. */
export type IconName =
  | "memory" | "map" | "branches" | "sun" | "moon"
  | "refresh" | "close" | "settings" | "plus"
  | "check" | "x" | "pin" | "undo";
